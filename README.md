# MEAN back end part one
Simple node server with 4 routes ['/' , '/users/register' , '/users/authenticate' , '/users/profile']

# Start node server
cd into project directory and use command 'nodemon'
Note: npm install -g nodemon